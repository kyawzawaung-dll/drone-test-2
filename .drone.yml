---
kind: pipeline
type: docker
name: default

steps:
  - name: lint
    image: python:3.7-alpine
    commands:
      - python -m pippp install -r requirements.txt
      - flake8

trigger:
  status:
  - failure
